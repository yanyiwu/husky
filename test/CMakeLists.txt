SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/test)

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR})
ADD_EXECUTABLE(husky.demo husky.cpp)
TARGET_LINK_LIBRARIES(husky.demo pthread)

ADD_SUBDIRECTORY(unittest)
